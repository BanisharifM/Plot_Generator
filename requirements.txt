streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.10  # boxplot tick_labels=/orientation= require >=3.10
scipy>=1.11.0     # histogram KDE, scatter regression
pyarrow>=15.0     # parquet upload support (was only transitive before)
openpyxl>=3.1.0   # .xlsx
xlrd>=2.0.0       # legacy .xls
duckdb>=1.0       # in-place SQL over CSV/Parquet (data layer)
